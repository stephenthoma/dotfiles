[user]
	name = stephenthoma
	email = stephen@stephenthoma.com
[alias]
	co = checkout
	rb = rebase
	st = status
	br = branch
    unstage = reset -q HEAD --
    discard = checkout --
    uncommit = reset --mixed HEAD~
    graph = log --graph -10 --branches --remotes --tags  --format=format:'%Cgreen%h %Creset• %<(75,trunc)%s (%cN, %ar) %Cred%d' --date-order
[diff]
    compactionHeuristic = true
    indentHeuristic = true
    algorithm = patience
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	excludesfile = ~/.global-gitignore
	editor = nvim
	autocrlf = input
[apply]
	whitespace = nowarn
[format]
	pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset
[blame]
    date = relative
[push]
	default = simple
[credential]
	helper = osxkeychain
[oh-my-zsh]
	hide-status = 0
